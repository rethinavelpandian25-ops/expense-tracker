{
  "name": "ledger-android",
  "version": "1.0.0",
  "private": true,
  "description": "Android app wrapper for Ledger — Expense & Budget Tracker (loads the live Flask app, no separate build step for the web app itself).",
  "scripts": {
    "add-android": "cap add android",
    "sync": "cap sync android",
    "open": "cap open android"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/android": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
